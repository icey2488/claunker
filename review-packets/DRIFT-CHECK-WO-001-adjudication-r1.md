# WO-001 round-1 adjudication

Adjudicated 2026-07-27 (Claude, architect seat) against the raw teardown in DRIFT-CHECK-WO-001-teardown-r1.md. Outcome: 2 findings stand as BLOCKER, 9 land as MAJOR (several reclassified down from BLOCKER because the fix is documentation, not redesign), 1 partial, 1 rejected as designed behavior. One adjudication-surfaced item (A1) added. Amendment set: DRIFT-CHECK-WO-001-amendments-v2.md, ratified by operator same day.

| F | Verdict | Severity | Disposition |
|---|---|---|---|
| F1 | PARTIAL | BLOCKER->MAJOR | Mechanism true; premise wrong. The hook's ratified job is checker liveness, not universal push-time interception — but the WO never says so. Amend Known limit: detection latency for drift pushed from other repos = next claunker-ops-side run. Reject the proposed fix: global core.hooksPath hijacks every repo on the machine and triggers F13 everywhere at once. |
| F2 | REJECT | — (MINOR doc note) | The "trap" is fix -> commit -> push — normal git. Committed-bytes-as-truth plus working-tree repair plus human commit is the intended authorization chain; auto-commit from a gated script is a writer we don't want. Accept only: --fix output must state "working tree updated, commit required before green." |
| F3 | ACCEPT | BLOCKER | Clean kill. Source files carry no propagation header, so literal "writes source over copy" either deletes the header (next run: unparseable, itself an unmapped condition) or, body-only, strands the old SHA (permanent false DRIFT). Amendment splits fix semantics: drift-fix = body := source@recorded-SHA, header untouched; staleness-fix = body := source@HEAD AND header SHA := HEAD, re-emitted in the manifest-declared format. |
| F4 | ACCEPT | BLOCKER->MAJOR | The design is sound — manifest drives REQUIRED/MISSING_COPY, filename-scan is the unmanifested-coverage net — but the WO states neither, and a faithful builder can build scan-as-discovery and make MISSING_COPY unreachable. Amend with the composition paragraph; the unmanifested-duplicate condition also needs an outcome (feeds F10). |
| F5 | ACCEPT | BLOCKER | The bite test bites one of four assertions. A script ignoring STALENESS, MISSING_COPY, and the freshness abort passes acceptance verbatim — green-on-fixtures without working-in-production. Amend: one red fixture per assertion (drift, staleness [header@A, source@B, body clean@A], missing-required, stale-freshness), each red-before-green. |
| F6 | ACCEPT | BLOCKER->MAJOR | True — git show resolves in cwd's object store; cross-repo needs git -C <path>. Downgraded: fails loud on first run, never silently. Amend: manifest declares per-repo path (relative to a configured root, not hardcoded absolute). |
| F7 | PARTIAL | BLOCKER->MAJOR | Fork topology doesn't exist here, but the surviving half is locally live: origin HAS been repointed on this rig before. Accept the narrow hardening — manifest pins canonical remote URL per repo, check asserts git remote get-url matches, no fetch. Drop the absolute-timestamp half. |
| F8 | ACCEPT | BLOCKER->MAJOR | The Boolean-AND reading yields absurdity (fresh fetch + known-behind = no abort), so it is not the intent — but a cold builder can implement it, and step 3 aside ("behind-origin" CAN be true on static refs), the ambiguity is real on the control's trigger. Amend one sentence: abort if EITHER behind-origin per last-fetched refs OR last fetch older than threshold. |
| F9 | ACCEPT | MAJOR | Directly relevant — this rig runs worktrees. Amend: resolve metadata via git rev-parse --git-common-dir, never stat .git literally. |
| F10 | ACCEPT | MAJOR | Conditions exceed outcomes: STALENESS, abort, unparseable-header (F3), unmanifested-duplicate (F4) all lack exit codes. Amend with a total outcome map + precedence generalizing "drift first, never suppressed." Touches the verbatim-ratified "Two exit codes" — card-level amendment, operator authorization obtained. |
| F11 | ACCEPT | MAJOR | Threshold needs a home and a starting value (manifest field; number is the operator's call). Miscalibration in either direction is load-bearing — too long passes stale, too short trains dismissal. |
| F12 | ACCEPT | MAJOR | The intent is the countable-pin discipline: manifest carries accepted_gap_count, checker fails when count != actual ACCEPTED_GAP entries, so widening the exemption set forces touching the number in the same diff. Buildable once written; unguessable from the WO — which is the point of the self-containedness rule. |
| F13 | ACCEPT | MAJOR | True: core.hooksPath replaces .git/hooks wholesale. Cheap verify-first gate: installation step inventories existing effective hooks and current hooksPath, confirms empty or chain-loads, records the result. |

A1 (adjudication-surfaced, not in the teardown): ACCEPTED_GAP's suppression scope is undefined. The day-one candidate (HARNESS-FIDELITY 27-byte header divergence) is a content divergence being accepted — so does an ACCEPTED_GAP entry suppress DRIFT, MISSING, or a declared byte-range only? As written, a builder could ship ACCEPTED_GAP-suppresses-everything, turning each pin into a total blind spot on that copy. Remedy: scope field per entry plus expected-divergence fingerprint (see AMD-7).

Disposition path: amendment set drafted as exact sentence-level changes keyed to finding IDs; operator ratified item-by-item authority with "Fire as is" 2026-07-27, including threshold starting value 168 and authorization for AMD-4's supersession of the card-verbatim "Two exit codes" wording. Applied to WO + source card 1486ea8a in one arc; v2 returns through a fresh packet for round 2 before the hooks build fires.

## Post-ratification apply note (2026-07-27)

The first apply attempt (job wo001-v2-apply-20260727) stopped clean at its pre-mutation gate with zero mutations: the card's requirement items are plain numbered prose without the bold labels the WO displays. The WO's "Quoted from the source card, verbatim" was label-augmented at drafting, not byte-verbatim, and the first apply order built its card anchors from the WO's rendering rather than the card's bytes — the same renderings-not-bytes failure this corpus documents, caught by the gate. A second attempt (wo001-v2-apply-20260727-2) never booted: the claude-async bridge passes the prompt to the runner via argv, and the enlarged order crossed the Windows command-line ceiling (spawn ENAMETOOLONG, exit 127) — a bridge limitation now on record: large payloads travel as committed files, not prompt bytes. The arc therefore completed as a split pair: wo001-v2-payloads-20260727 committed the round-1 artifacts (this file among them), and wo001-v2-apply-20260727-3 applied the amendments taking each AMD's payload from this committed file's fenced blocks — bytes from git, not from prompt — anchored the card surgery on the card's actual item openings, and wrote the ratified labeled text to both card and WO, making the verbatim claim true going forward for items 2, 3, and 5. Residual known divergence: items 1 and 4 and the constraints list remain unlabeled prose on the card while the WO shows bold labels, so the WO's blanket verbatim claim stays soft there — left for round-2 review or a separate operator-authorized alignment pass, not silently fixed in this arc.
