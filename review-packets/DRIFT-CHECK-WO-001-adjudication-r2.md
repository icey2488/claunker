# WO-001 round-2 adjudication

Adjudicated 2026-07-28 (Claude, architect seat) against the raw teardown in DRIFT-CHECK-WO-001-teardown-r2.md. Base-wording-dependent findings (F5, F9) verified against the committed WO bytes, not memory. Headline: both BLOCKERs die as designs and survive as clarifications — no design-invalidating flaw in v2; findings quality shifted from round 1's "this mechanism cannot work" to "this sentence permits a wrong build," which is what convergence looks like. Amendment set: DRIFT-CHECK-WO-001-amendments-v3.md.

| F | Verdict | Severity | Disposition |
|---|---|---|---|
| F1 | ACCEPT | BLOCKER->MAJOR | Real silent-pass: behind-origin and STALENESS are branch-relative and the WO never names the branch — on a rig running worktrees and feature branches, HEAD-defaulting evaluates the wrong ref. Same shape as round-1 F8 (ambiguity permitting a wrong build, not forcing one). Amend: manifest per-repo canonical_branch (main); all source-side evaluations pin to it, never HEAD. |
| F2 | PARTIAL | BLOCKER->MAJOR | The "impossible logic" reading assumes per-copy precedence. Precedence ranks DETECTED conditions run-wide for exit selection; an unparseable header makes drift UNEVALUATED for that copy — not detected, not suppressed. No fake SHA required. The flip is rejected: a run with one bad header and a detected drift on another copy should exit DRIFT. The per-copy/run-wide distinction is genuinely unstated — amend with a clarifying paragraph. |
| F3 | ACCEPT | MAJOR | The wording trap is real: "provenance header absent" literally covers a nonexistent file. Amend the HEADER_UNPARSEABLE row: file exists but header absent/unparseable; a missing file is MISSING_COPY, never HEADER_UNPARSEABLE. Reordering half rejected — with per-copy disambiguation and all-conditions-reported, exit ordering between them is low stakes. |
| F4 | ACCEPT | MAJOR | Scan universe genuinely undefined. Fix differs from the proposal: no second list — the manifest's per-repo entries ARE the scan universe; a repo absent from the manifest is outside governance, recorded as a known limit. Two lists (repos + monitored_repositories) is a drift pair waiting to happen. |
| F5 | ACCEPT | MAJOR | "Governed-looking" is undefined and Constraint 1 (verified bytes: scope is duplicate-filename detection, not globs, not a governance/ directory) removes the obvious crutches. Define: basename exactly matches a manifest-declared governed document's filename. Exact-match against the manifest set is neither glob nor directory convention — Constraint 1 stands. Header-pattern alternative rejected: a drifted duplicate may lack a header. |
| F6 | ACCEPT, fix REJECTED | MAJOR | Best catch of the round. Mechanism real: staleness-fix erases the accepted divergence; fingerprint mismatch fires false DRIFT next run. But auto-purging the manifest is exactly wrong — a script silently editing a counted-pin control surface is what counted pins exist to prevent (purge-without-decrement trips PIN_MISMATCH; purge-with-decrement is a script rewriting governance). Amend: --fix REFUSES on any copy carrying an ACCEPTED_GAP; resolution is an operator manifest diff. Generalized: the script never writes the manifest — sibling of "never stages or commits." |
| F7 | ACCEPT | MAJOR | Round-1 F12's lesson, next field over. Amend with a pinned pipeline: materialize source@recorded-SHA via git show to temp; git diff --no-index --no-color; strip path-bearing header lines (diff --git, index, ---, +++); SHA-256 the remainder; store hex. Fully deterministic. |
| F8 | PARTIAL | MAJOR | Not a discovered hole — 2c450718 is the WO's declared open dependency (Out-of-scope excludes ratifying it, verified bytes). The self-containedness tension is real and resolvable better than "block everything": fixtures declare their own header_format, so the checker and full bite test build NOW; only production manifest values and real-copy --fix header re-emission block on 2c450718. That makes 2c450718 load-bearing for go-live, not for the build. |
| F9 | ACCEPT | MAJOR | Verified bytes: "Touching kanbantt-app" sits under "Explicitly excluded from this work order" — build scope, not runtime scope. A hyper-literal builder excluding it from the scan blinds the check's main purpose. Amend: the exclusion means this build ships no changes to kanbantt-app; the shipped checker reads it (scan + git show); runtime --fix on a kanbantt-app copy stays under confirm-and-operator-commits. |
| F10 | ACCEPT | MAJOR | Round-1 F5's green-on-fixtures lesson applied to the worktree requirement — correct, and the catch-all fixture bullet does not cover it (worktree handling is an implementation requirement, not an outcome-map condition). Amend: at least one fixture exercised through a linked worktree, red-before-green — a literal-.git-stat build must fail it. |
| F11 | ACCEPT, fix narrowed | MINOR | Real ambiguity; "match any remote" rejected (a stale fork on upstream matching the pin would reopen round-1 F7). Amend: the pin names the remote — origin must match canonical_remote_url; freshness refs read from origin/<canonical_branch>; absent or mismatched origin -> REMOTE_MISMATCH. |
| F12 | ACCEPT | MINOR | True — hooksPath does not clone. Amend: name the entry point (install script shipped with WO-001 in claunker-ops, invoked manually once per clone) and extend the known limit: a fresh clone has no hook until install runs. |

A2 (adjudication-surfaced, not in the teardown): the canonical-branch pin (F1) must sweep every "source HEAD" reference — the outcome map's STALENESS row and the staleness-fix target (body := source@canonical-branch-tip) — or the fix path inherits the exact checked-out-branch ambiguity F1 kills at the check path.

Disposition path: 13 amendments (AMD-11..23, continuing round-1 numbering), all specification-tightening, strictness up-or-neutral; two touch card-verbatim Requirements text (AMD-11 -> Req 2, AMD-12 -> Req 5), card rides along as in round 1 with --expected-version and edit_audit. F8's disposition makes 2c450718 ratification the go-live gate rather than a build gate. Operator ratified the dispositions in-session 2026-07-28 in the operator's own voice: "Accepted. Proceed with v3 amendment" — sentence-level drafting delegated to the architect within the ratified dispositions, card ride-along explicitly covered by the accepted adjudication. Applied via the split-pair mechanics (payloads as committed files per the ENAMETOOLONG record); whether v3 gets a round 3 or proceeds to build is the operator's call on seeing the applied set.
